(display (char=? #\A #\B))
(newline)

(display (char=? #\a #\b))
(newline)

(display (char=? #\9 #\0))
(newline)

(display (char=? #\A #\A))
(newline)

(display (char<? #\A #\B))
(newline)

(display (char<? #\a #\b))
(newline)

(display (char<? #\9 #\0))
(newline)

(display (char<? #\A #\A))
(newline)

(display (char>? #\A #\B))
(newline)

(display (char>? #\a #\b))
(newline)

(display (char>? #\9 #\0))
(newline)

(display (char>? #\A #\A))
(newline)

(display (char<=? #\A #\B))
(newline)

(display (char<=? #\a #\b))
(newline)

(display (char<=? #\9 #\0))
(newline)

(display (char<=? #\A #\A))
(newline)

(display (char>=? #\A #\B))
(newline)

(display (char>=? #\a #\b))
(newline)

(display (char>=? #\9 #\0))
(newline)

(display (char>=? #\A #\A))
(newline)

(display (char-ci=? #\A #\B))
(newline)

(display (char-ci=? #\a #\B))
(newline)

(display (char-ci=? #\A #\b))
(newline)

(display (char-ci=? #\a #\b))
(newline)

(display (char-ci=? #\9 #\0))
(newline)

(display (char-ci=? #\A #\A))
(newline)

(display (char-ci=? #\A #\a))
(newline)

(display (char-ci<? #\A #\B))
(newline)

(display (char-ci<? #\a #\B))
(newline)

(display (char-ci<? #\A #\b))
(newline)

(display (char-ci<? #\a #\b))
(newline)

(display (char-ci<? #\9 #\0))
(newline)

(display (char-ci<? #\A #\A))
(newline)

(display (char-ci<? #\A #\a))
(newline)

(display (char-ci>? #\A #\B))
(newline)

(display (char-ci>? #\a #\B))
(newline)

(display (char-ci>? #\A #\b))
(newline)

(display (char-ci>? #\a #\b))
(newline)

(display (char-ci>? #\9 #\0))
(newline)

(display (char-ci>? #\A #\A))
(newline)

(display (char-ci>? #\A #\a))
(newline)

(display (char-ci<=? #\A #\B))
(newline)

(display (char-ci<=? #\a #\B))
(newline)

(display (char-ci<=? #\A #\b))
(newline)

(display (char-ci<=? #\a #\b))
(newline)

(display (char-ci<=? #\9 #\0))
(newline)

(display (char-ci<=? #\A #\A))
(newline)

(display (char-ci<=? #\A #\a))
(newline)

(display (char-ci>=? #\A #\B))
(newline)

(display (char-ci>=? #\a #\B))
(newline)

(display (char-ci>=? #\A #\b))
(newline)

(display (char-ci>=? #\a #\b))
(newline)

(display (char-ci>=? #\9 #\0))
(newline)

(display (char-ci>=? #\A #\A))
(newline)

(display (char-ci>=? #\A #\a))
(newline)

;;;options: -l r4rs
;;;expected:
;;;#f
;;;#f
;;;#f
;;;#t
;;;#t
;;;#t
;;;#f
;;;#f
;;;#f
;;;#f
;;;#t
;;;#f
;;;#t
;;;#t
;;;#f
;;;#t
;;;#f
;;;#f
;;;#t
;;;#t
;;;#f
;;;#f
;;;#f
;;;#f
;;;#f
;;;#t
;;;#t
;;;#t
;;;#t
;;;#t
;;;#t
;;;#f
;;;#f
;;;#f
;;;#f
;;;#f
;;;#f
;;;#f
;;;#t
;;;#f
;;;#f
;;;#t
;;;#t
;;;#t
;;;#t
;;;#f
;;;#t
;;;#t
;;;#f
;;;#f
;;;#f
;;;#f
;;;#t
;;;#t
;;;#t
