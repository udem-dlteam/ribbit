#!/bin/sh

if [ "$LIB" = "" ]; then 
    LIB="-f+ v-port -l r4rs -l r4rs/sys"
fi

if [ "$HOST" = "" ]; then 
    HOST="js"
fi

gsi -:r4rs ./rsc.scm -t "$HOST" $LIB "$@" -o ./rsi."$HOST" ./lib/r4rs/rsi.scm
